# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=your-secret-key-here

# MongoDB Configuration
MONGODB_DB=medical_records
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_USERNAME=your_username
MONGODB_PASSWORD=your_password

# JWT Configuration
JWT_SECRET_KEY=jwt-secret-key-here
JWT_ACCESS_TOKEN_EXPIRES=3600



# For local MongoDB installation
# client = MongoClient('mongodb://localhost:27017/')

# For MongoDB Atlas (replace <username>, <password>, <cluster-url>, and <dbname> with your actual values)
# client = MongoClient('mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority')

db = client.medical_records